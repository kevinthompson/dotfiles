# Commands
eval "$(rbenv init - zsh)"
eval "$(hub alias -s)"

# Unix
alias zr="source ~/.zlogin && source ~/.zshrc"
alias e="$EDITOR"
alias v="$VISUAL"
alias vi="$VISUAL"
alias vim="$VISUAL"
alias ae="v ~/.zsh/aliases"
alias c="clear"
alias l='ls'
alias ll='ls -al'
alias lh='ls -Alh'
alias mkdir='mkdir -p'

# Bundler
alias b="bundle"
alias be="bundle exec"

# Rubygems
alias gi="gem install"
alias giv="gem install -v"

# Rails
alias lg="tail -f log/development.log"
alias migrate="rake db:migrate db:rollback && rake db:migrate db:test:prepare"
alias s="rspec"

# Git
alias gs="git status"
alias gps="git push"
alias gpl="git fetch && git pull --rebase"
alias gb="git branch"
alias gf="git fetch"
alias gd="git diff"
alias gap="git add -p"
alias gc="git commit"
alias gco="git checkout"
alias gpr="git pull-request"
alias gbr="git browse"
alias gl="git log --pretty=oneline --abbrev-commit"
alias gt="!git clone --depth 1 --origin source"
alias staging="gco staging; gf origin staging; git reset --hard origin/staging"
alias master="gco master; gpl"

# Tmux
alias tn="tmux -S /tmp/pair new -s"
alias ta="tmux -S /tmp/pair attach -t"
alias tl="tmux -S /tmp/pair ls"
alias tk="tmux -S /tmp/pair kill-session -t"

# Apache
alias vhosts="v /etc/apache2/extra/httpd-vhosts.conf"

# Misc
alias x="exercism"
alias ex="elixir"
